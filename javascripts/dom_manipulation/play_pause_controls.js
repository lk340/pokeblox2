export const playPause = () => {
  document.addEventListener("keydown", event => {
    switch(event.which) {
      case 81: // q
      case 69: // e
      case 82: // u
      case 73: // i
    }
  });
};